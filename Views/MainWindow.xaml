<Window x:Class="SASB_SI_ASSESSMENT.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:behaviors="clr-namespace:SASB_SI_ASSESSMENT.Behaviors"
        Title="IoT Dashboard" Height="500" Width="900">

    <!-- Merge your external style dictionary -->
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Styles/Controls.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Input section -->
        <StackPanel Orientation="Horizontal" Margin="10" Grid.Row="0">
            <TextBox Width="200"
                     Margin="0,0,5,0"
                     Text="{Binding NewDeviceName, UpdateSourceTrigger=PropertyChanged}"/>
            <ComboBox Width="150"
                      Margin="0,0,5,0"
                      ItemsSource="{Binding DeviceTypes}"
                      SelectedItem="{Binding NewDeviceType}"/>
            <Button Content="Add Device"
                    Style="{StaticResource GridButtonStyle}"
                    Command="{Binding AddDeviceCommand}"/>
        </StackPanel>

        <!-- Device List & Logs -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Device List -->
            <ListView ItemsSource="{Binding Devices}" Grid.Column="0" SelectedItem="{Binding SelectedDevice}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="ID" DisplayMemberBinding="{Binding Id}" Width="Auto"/>
                        <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="Auto"/>
                        <GridViewColumn Header="Type" DisplayMemberBinding="{Binding Type}" Width="Auto"/>
                        <GridViewColumn Header="Status" Width="Auto">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Status}"
                                               Style="{StaticResource StatusTextBlockStyle}"
                                               Foreground="{Binding Status, Converter={StaticResource StatusToBrushConverter}}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Last Updated" DisplayMemberBinding="{Binding LastUpdated}" Width="Auto"/>
                        <GridViewColumn Header="Actions" Width="Auto">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="Update"
                                            Style="{StaticResource GridButtonStyle}"
                                            Command="{Binding UpdateCommand}"/>
                                        <Button Content="Delete"
                                            Style="{StaticResource GridButtonStyle}"
                                            Command="{Binding DeleteCommand}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Header="Power" Width="Auto">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Style="{StaticResource PowerButtonStyle}"
                                        Command="{Binding TogglePowerCommand}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>

            <!-- System Logs -->
            <ListBox ItemsSource="{Binding SystemLogs}"
                     Grid.Column="1"
                     behaviors:AutoScrollBehavior.IsEnabled="True"
                     HorizontalContentAlignment="Stretch">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Style="{StaticResource LogTextStyle}"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Grid>
</Window>
